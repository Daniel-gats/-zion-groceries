CREATE TABLE products (id SERIAL PRIMARY KEY, name VARCHAR(255) NOT NULL, price DECIMAL(10,2) NOT NULL, category VARCHAR(50) NOT NULL, image TEXT, unit VARCHAR(50) DEFAULT 'kg', stock INTEGER DEFAULT 100, description TEXT, created_at TIMESTAMP DEFAULT NOW());
